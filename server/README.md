# OfflineAI School API

Pilot synchronization backend.

## Run
cd server
npm install
npm start

Endpoints: GET /health, POST /v1/sync/attempts, GET /v1/learners/:id/progress.

Before real student data is used, add authentication, authorization, TLS, encryption, rate limiting, audit logging, production hosting, backups and privacy controls.